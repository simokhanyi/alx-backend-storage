//Get the database name from the command line argument
db_name="$1"

// Execute the MongoDB find command to list all documents in the school collection
mongo "$db_name" --quiet --eval 'db.school.find().forEach(printjson)'
